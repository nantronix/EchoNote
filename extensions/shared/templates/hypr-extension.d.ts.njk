/**
 * EchoNote Extension Runtime Types
 *
 * AUTO-GENERATED - DO NOT EDIT MANUALLY
 * Run: deno task generate
 */

// @echonote/store - {{ store.description }}
declare module "@echonote/store" {
  import type * as _UI from "tinybase/ui-react/with-schemas";
  import type { TablesSchema, ValuesSchema } from "tinybase/with-schemas";

  export const SCHEMA: {
    value: ValuesSchema;
    table: TablesSchema;
  };

  export type Schemas = [typeof SCHEMA.table, typeof SCHEMA.value];

{% for name, config in store.exports %}
  export const {{ name }}: {{ config.type }};
{% endfor %}
}

// @echonote/tabs - {{ tabs.description }}
declare module "@echonote/tabs" {
{% for name, config in tabs.exports %}
  export const {{ name }}: {{ config.type }};
{% endfor %}
}

// @echonote/ui - UI components (shadcn-style)
{% for module in uiModules %}
declare module "@echonote/ui/{{ module.subpath }}" {
  import type * as React from "react";
{% for exp in module.exports %}
  export const {{ exp }}: React.ComponentType<Record<string, unknown>>;
{% endfor %}
}

{% endfor %}
// echonote aggregate namespace
declare module "echonote" {
  import type * as React from "react";
  import type * as _UI from "tinybase/ui-react/with-schemas";
  import type { TablesSchema, ValuesSchema } from "tinybase/with-schemas";

  type StoreSchema = {
    value: ValuesSchema;
    table: TablesSchema;
  };
  type Schemas = [StoreSchema["table"], StoreSchema["value"]];

  export const store: {
{% for name, config in store.exports %}
    {{ name }}: {{ config.type }};
{% endfor %}
  };
  export const tabs: {
{% for name, config in tabs.exports %}
    {{ name }}: {{ config.type }};
{% endfor %}
  };
  export const ui: {
{% for module in uiModules %}
    {{ module.alias }}: {
{% for exp in module.exports %}
      {{ exp }}: React.ComponentType<Record<string, unknown>>;
{% endfor %}
    };
{% endfor %}
  };
  export interface ExtensionViewProps {
    extensionId: string;
    state?: Record<string, unknown>;
  }
}

// Global extension props (for convenience)
interface ExtensionViewProps {
  extensionId: string;
  state?: Record<string, unknown>;
}

